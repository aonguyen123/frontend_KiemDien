const styles = theme => ({
    root: {},
    changeButton: {
        marginRight: theme.spacing(5)
    },
    content: {
        marginTop: theme.spacing(-2)
    },
    info: {
        padding: theme.spacing(2)
    }
});
export default styles;