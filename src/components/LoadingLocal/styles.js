const styles = theme => ({
    loadingCenterCard: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: theme.spacing(3)
    }
});
export default styles;